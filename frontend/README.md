# Portal Demo — Frontend

Svelte + Vite frontend for the Portal Demo application.

## Development

```bash
npm install
npm run dev
```

Requires the backend to be running (`REST_WS_ENDPOINT`, `REST_TOKEN` set). See the [root README](../README.MD) for full setup instructions.
